spring:
  application:
    name: ai

  datasource:
    url: jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres?sslmode=require
    username: postgres.oaglulyfmregzeogmyow
    password: paatrikmatos

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  openai:
    api-key: ${OPENAI_API_KEY}

server:
  port: 8080